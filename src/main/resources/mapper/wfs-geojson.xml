<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.mapservice.mapper.WfsMapper">
    <select id="convenienceStore" resultType="String">
        select geojson from map.v_convenience_store_geojson
    </select>
    <select id="busStopInfo" resultType="String">
        select geojson from map.v_bus_stop_info_geojson
    </select>
    <select id="cctvInfo" resultType="String">
        select geojson from map.v_cctv_info_geojson
    </select>
</mapper>
